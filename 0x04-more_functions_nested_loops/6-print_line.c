#include "main.h"
/**
 * print_line -print a straight line
 * @n: the number of line to draw
 * Return: void
 */
void print_line(int n)
{
	for (int i = 1; i <= n; i++)
	{
	if 	(n <= 0)
	_putchar('\n');
	else
	_putchar(95);
    }
_putchar('\n')
}
