#include "main.h"
/**
 * mul - multiplies two integers
 * @a: int the first nuber to multiply
 * @b: int the second number to multiply
 * Return: the product of the two numbers
 */
int mul(int a, int b)
	{
	return (a * b);
	}
